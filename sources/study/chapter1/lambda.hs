-- 𝜆𝑥.𝑥
-- ^-^-------- extend of the head of the lambda.

-- 𝜆𝑥.𝑥
--  ^--------- the single parameter of the function. This binds any variables with the same name in the body of the function.

-- 𝜆𝑥.𝑥
--    ^------- body, the expression the lambda returns when applied. This is a bound variables.

-- 𝜆𝑥.𝑥
-- 𝜆𝑦.𝑦
-- 𝜆𝑧.𝑧
-- All the same function.

-- 𝜆𝑥.𝑥𝑦
--     ^ This is a free variables. The body expression has variables that are not named in the head.